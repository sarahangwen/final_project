services:
  - type: web
    name: kglppro-backend
    env: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE
        fromDatabase:
          name: kglppro-mongodb
          property: connectionString
